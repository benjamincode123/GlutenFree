/** Minimum characters required before product name search runs (app + API). */
export const MIN_PRODUCT_SEARCH_CHARS = 4;

/** Results per page on the product search screen. */
export const PRODUCT_SEARCH_PAGE_SIZE = 15;
